server:
  port: 7777
logging:
  config: classpath:log4j2.xml
  level:
    root: INFO
spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: file:///H:/MY_OWN_CODE_FILE/universal-wall-refactor/others/config/{application}
  application:
    name: configserver